import setuptools

setuptools.setup(
    name='pycudaprof',
    version='0.0.1',
    author='Nikoli Dryden',
    author_email='ndryden@ndryden.com',
    description='Python interface to CUDA profiler control',
    packages=setuptools.find_packages(),
    python_requires='>=3.6'
)
